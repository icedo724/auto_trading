# --- core (필수) ---
pandas>=2.0
numpy>=1.24
PyYAML>=6.0

# --- 데이터 소스 (선택, 전부 무료) ---
# 국내 권장: 무인증 · KRX/미국/지수/환율을 한 인터페이스로
finance-datareader>=0.9.50
# 미국/글로벌
yfinance>=0.2.40
# KRX 고유 데이터(투자자별 매매동향, 공매도 등). 로그인은 선택
pykrx>=1.0.45
# 'naver' 소스는 추가 패키지 없이 requests 만으로 동작한다

# --- 리포트 차트 (선택) ---
matplotlib>=3.7
